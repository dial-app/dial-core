[tox]
envlist = py36,py37,py38

[testenv]
passenv = CI TRAVIS TRAVIS_*
deps =
    -rrequirements.txt
    -rrequirements-dev.txt
commands =
    pytest --cov=tfg --cov-append

[testenv:clean]
deps = coverage
skip_install = true
commands = coverage erase
